export const getAuth = (state) => state.usuario.isAuth;

export const getNombres = (state) => state.usuario.nombres;

export const getRol = (state) => state.usuario.rol;

export const getCorreo = (state) => state.usuario.correo;
